apiVersion: v1
kind: Service
metadata:
  name: postnummer-api-entrypoint
  namespace: app-postnummer
  labels:
    app: postnummer-api
spec:
  type: ClusterIP
  selector:
    app: postnummer-api
  ports:
    - name: http-80
      protocol: TCP
      port: 80
      targetPort: 80